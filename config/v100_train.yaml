Training:
  working_directory: '/mnt/nerf-data/batches'  # directory for intermediate data conversion
  n_epochs: 1000                                          # number of updates for all available rays
  batch_size: 1024                                        # number of rays per gradient step (power of 2)
  num_workers: 32                                         # number of workers for data loading
  log_every_n_steps: 10000                                # log images and save states between epochs

Lambda:
  regularization: 1

Debug: True